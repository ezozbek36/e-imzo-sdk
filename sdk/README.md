# SDK Area

This directory is reserved for future SDK implementation artifacts.

Current repository mode remains specification-first.
Implementation work should begin only after specification and validation layers are sufficiently stable.
